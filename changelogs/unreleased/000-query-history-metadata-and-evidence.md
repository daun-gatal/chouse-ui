type: minor

### Added
- **Metadata-backed Explorer history** — Sync each user's bounded query execution history to the metadata database while retaining immediate local access and importing existing browser-local entries.

### Fixed
- **Data Health evidence windows** — Inspect the actual evaluated schedule window and explain when a passing check has no violating rows instead of showing an unexplained empty table.
