type: minor

### Changed
- **Scoped Data Permissions** — Data Access policy rules now scope database, query, and table operations per connection/database/table while roles remain the maximum permission ceiling.
- **Role Data Access Preview** — roles now show the effective Data Access rule order when multiple policies are attached, including allow/deny precedence, priority, scope, and operation counts.
