# ============================================
# ClickHouse Studio - Environment Configuration
# ============================================
# Copy this file to .env and configure for your environment
# cp .env.example .env

# --------------------------------------------
# Server Configuration
# --------------------------------------------
NODE_ENV=development
PORT=5521
STATIC_PATH=./dist

# Session timeout in milliseconds (default: 1 hour)
SESSION_TTL=3600000

# CORS allowed origins (comma-separated, or * for all)
# Production: Set to your specific domain(s)
CORS_ORIGIN=*

# --------------------------------------------
# RBAC Database Configuration
# --------------------------------------------
# Database type: sqlite or postgres
RBAC_DB_TYPE=sqlite

# SQLite configuration (used when RBAC_DB_TYPE=sqlite)
RBAC_SQLITE_PATH=./data/rbac.db

# PostgreSQL configuration (used when RBAC_DB_TYPE=postgres)
# RBAC_POSTGRES_URL=postgres://user:password@localhost:5432/chstudio
# RBAC_POSTGRES_POOL_SIZE=10

# --------------------------------------------
# Security Configuration
# --------------------------------------------
# JWT secret for token signing (REQUIRED - use a strong random string)
# Generate with: openssl rand -base64 32
RBAC_JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Token expiration times
RBAC_ACCESS_TOKEN_EXPIRES_IN=15m
RBAC_REFRESH_TOKEN_EXPIRES_IN=7d

# AES-256 encryption key for connection passwords (REQUIRED - exactly 32 characters)
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=your-32-character-encryption-key

# Default admin password (only used on first initialization)
RBAC_ADMIN_PASSWORD=admin123

# --------------------------------------------
# Frontend Configuration (Vite)
# --------------------------------------------
# Base path for the application (useful for reverse proxies)
# VITE_BASE_PATH=/studio

# --------------------------------------------
# DEPRECATED - Legacy ClickHouse Configuration
# --------------------------------------------
# These are no longer used. Configure ClickHouse connections
# via the Admin > Connections panel instead.
# CLICKHOUSE_DEFAULT_URL=
# CLICKHOUSE_PRESET_URLS=
# CLICKHOUSE_DEFAULT_USER=
# VITE_CLICKHOUSE_URLS=
# VITE_CLICKHOUSE_PASS=
